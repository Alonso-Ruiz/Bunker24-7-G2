# Example environment variables for the Spring Boot backend
# Do NOT commit real credentials to git. Use this file as a template.

# Convert Supabase database URL from "postgres://user:pass@host:port/dbname"
# to JDBC format: jdbc:postgresql://host:port/dbname

# Example (replace with your values):
# SPRING_DATASOURCE_URL=jdbc:postgresql://db.bqegrv...supabase.co:5432/postgres
# SPRING_DATASOURCE_USERNAME=postgres
# SPRING_DATASOURCE_PASSWORD=your_password_here

# PowerShell (session) example:
# $env:SPRING_DATASOURCE_URL = 'jdbc:postgresql://<host>:5432/<database>'
# $env:SPRING_DATASOURCE_USERNAME = '<user>'
# $env:SPRING_DATASOURCE_PASSWORD = '<password>'

# Then run the backend with the included Maven wrapper:
# cd backend
# .\mvnw.cmd spring-boot:run
