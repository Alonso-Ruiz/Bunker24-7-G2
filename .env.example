# Example environment variables for frontend (do NOT commit secrets)
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-public-key

# If you have other env vars for the frontend, list them here.
# NOTE: Never store the Supabase service_role key in the frontend.
# For server/backend, keep service_role in server env only.
